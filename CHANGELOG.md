# 更新记录

本仓库方法论的每次实质变更记录在此，新条目追加在顶部。

## 2026-09-11 · 模式 B 新增「主动提醒交接触发器」

**新增**
- `skills/handoff/` 模式 B：新增对话健康触发器——满足任一条件时 AI 主动提醒收尾交接：
  - 自上次收尾后又推进了 1 个完整阶段；
  - 当前对话已覆盖 ≥3 个互不相关的新主题（串味信号：新话题与交接文档事项名对不上）；
  - 用户明确表达告一段落（"今天到这吧""先这样"——此类话语直接走收尾协议，不只是提醒）。
- 频率上限：同一对话最多提醒 2 次，用户无视即不再提；提醒本身不算收尾，只有用户确认或发口令才动交接文档。

**调整（防复抄分叉）**
- `skills/project-bootstrap/` §3「收尾触发」第 2 层改为指向 handoff 的触发器——原先两处各写一套且频率阈值不一致（1 次 vs 2 次），现收敛为单一权威 + 指针。

**文档**
- README：「两句话接管全局」补一条功能说明（防聊过头会自动提醒）。
- CREDITS：补「四层工作边界」（BV1j3YL6oEvs）一讲的思想致谢，并注明触发器为原创实现。
- SANITIZE-REPORT：登记本次母本 → 发布版的 5 处泛化差异。

**兼容性**
- 纯增量，向后兼容：口令协议不变；已生成的活文档「〇、协作约定」无需改动。

**起因**
- 长对话反复自动压缩会失真（前一两轮尚可，多压几轮面目全非），而口令是主通道、用户常忘记发——需要 AI 侧兜底。
